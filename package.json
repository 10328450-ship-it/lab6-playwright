{
  "name": "lab6-playwright",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "start": "http-server public -p 3000",
    "update-snapshots": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "http-server": "^14.1.1"
  }
}